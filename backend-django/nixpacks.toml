[phases.build]
cmds = [
    "pip install -r requirements.txt",
    "python manage.py collectstatic --noinput",
]

[start]
cmd = "python manage.py migrate && gunicorn config.wsgi --bind 0.0.0.0:${PORT:-8000} --workers 2"
