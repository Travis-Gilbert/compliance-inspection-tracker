# Field Mapping Workflow Port

This compatibility path now points to `docs/FIELD_AND_MODEL_MAPPING.md`.

The canonical mapping artifact lives there so the filename matches the current
field and model mapping plan for the compliance workbench.
