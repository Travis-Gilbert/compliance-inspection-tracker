# Unified Workbench Plan

This compatibility path now points to `docs/UNIFIED_COMPLIANCE_WORKBENCH_PLAN.md`.

The canonical plan lives there so the filename matches the current workbench
consolidation artifact requested in `Compliance update.md`.
